module.exports = function(app) { app.get('/api/issues/counts', (req, res) => { return res.status(200).json({ status: 'Sovereign Issue Sockets Active', totalTrackedConflicts: 0, activeTreeSyncBlocked: false, provenanceTag: 'Brand Knowledge +0' }); }); };
