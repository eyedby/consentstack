const fs = require('fs'); const { Keypair } = require('@solana/web3.js'); function run() { console.log('========================================\n?? GENERATING CRYPTOGRAPHIC DEVNET KEY\n========================================'); const newWallet = Keypair.generate(); const keyArray = Array.from(newWallet.secretKey); fs.writeFileSync('./key.txt', JSON.stringify(keyArray)); console.log('?? SUCCESS: Valid Ed25519 Secret Key Written to key.txt!'); console.log('?? New Public Address Created:', newWallet.publicKey.toBase58()); console.log('\n?? NEXT STEP:\n1. Copy the public address above.\n2. Open your web browser and request a devnet airdrop faucet drop to it.\n3. Run: node mint-delta9.js'); } run();
